server:
  port: 8083


  config:
    activate:
      on-profile: dev
    name: bookstore-nginx-coupon-dev
    url: jdbc:h2:mem:bookstore-db-prod;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE  # prod DB 설정
    driver-class-name: org.h2.Driver
    username: sa
    password:  # 비밀번호를 빈 문자열로 두었음

#    url: jdbc:mysql://localhost:3306/plzbuybook_coupon_db
#    username: test #root
#    password: 1234 #21045
#    driver-class-name: com.mysql.cj.jdbc.Driver
#      ddl-auto: update  # 애플리케이션 실행 시 DB 스키마 자동 업데이트
#    show-sql: true  # 실행되는 SQL을 콘솔에 출력

  h2:
    console:
      enabled: true  # H2 콘솔 활성화
      path: /h2-console  # H2 콘솔 접근 경로

logging:
  level:
    root: DEBUG


eureka:
  client:
    register-with-eureka: true  # Eureka에 서비스 등록
    fetch-registry: true  # Eureka에서 서비스 목록 가져오기
    service-url:
      defaultZone: http://localhost:8761/eureka  # Eureka 서버 URL

---
server:
  port: 8083
spring:
  config:
    activate:
      on-profile: prod
  application:
    name: bookstore-nginx-coupon-prod
#  datasource:
#    url: jdbc:mysql://localhost:3306/plzbuybook_coupon_db
#    username: root
#    password: 21045
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect


logging:
  level:
    root: INFO


eureka:
  client:
    register-with-eureka: true  # Eureka에 서비스 등록
    fetch-registry: true  # Eureka에서 서비스 목록 가져오기
    service-url:
      defaultZone: http://localhost:8761/eureka  # Eureka 서버 URL

#      ddl-auto: create-drop # 엔티티로 등록도니 클래스와 매핑된 테이블 자동생성 (애플리케이션 종료시 테이블 삭제) **주의** 운영 설정은 update 로 변경할 것
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
#        show_sql: true # 쿼리문 콘솔에 출력
#        format_sql: true # 콘솔에 추가
#        use_sql_comments: true # SQL 내부에 주석추가
#server:
#  port: 8083 # 쿠폰서버 포트번호